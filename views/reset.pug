extends layouts/layout_login

block content
    .container-fluid
        .row.justify-content-center
            .col-sm-12.col-md-8.col-xl-4
                .form-container 
                    h1.text-center.pt-2 Cambiar contraseña
                    include partials/messages.pug
                    form(action = '/users/reset/:token' method='POST')
                        .form-group
                            input(type="password", class="form-control", name="password" placeholder="Nueva Contraseña", value="", required)
                        .form-group
                            input(type="password", class="form-control" name="confirm", placeholder="Confirmar Contraseña", value="", required)
                        .form-group
                            button.btn.btn-primary.btn-block(type="submit") Actualizar contraseña