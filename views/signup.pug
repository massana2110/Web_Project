extends layout

block content
    .container-fluid
        .row.justify-content-center
            .col-sm-6.col-md-4
                form(action="#")
                    .form-group
                        label(for="name") Nombre
                        input(
                            type="text"
                            class="form-control"
                            placeholder="David"
                        )
